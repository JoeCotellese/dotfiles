# TODO: If I set the editor to VIM
# then the Ctrl+a and Ctrl+e don't work in tmux

# export EDITOR=vim
# export VISUAL="$EDITOR"
# if EDITOR=vi then keybindings are vi-like
# I don't want that everywhere so force them 
# back to emails
# set -g mode-keys emacs
# set -g status-keys emacs
